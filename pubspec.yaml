name: flutter_voz_texto
description: A new Flutter project for Voice to Text using IBM Watson.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  permission_handler: ^11.0.0
  record: 4.4.4
  path_provider: ^2.1.1
  google_fonts: ^6.1.0
  intl: ^0.18.1
  uuid: ^4.0.0
  web_socket_channel: ^2.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
